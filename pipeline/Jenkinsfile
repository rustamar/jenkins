pipeline {
    agent { label 'master' }
    options { timestamps() }
    stages {
        stage('Create') {
            steps {
                jobDsl scriptText: '''freeStyleJob("Publish HTML") {
                }'''
            }
        }
        stage('Build') {
            steps {
                build 'Publish HTML'
            }
        }
    }
    post {
        always {
            echo 'Done!'
        }
    }
}
